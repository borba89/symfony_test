{{ include('layout/header.html.twig') }}
<div class="container">
<nav class="nav mt-5">
    <a class="nav-link" href="{{ path('discipline') }}">Disciplines</a>
    <a class="nav-link" href="{{ path('teachers') }}">Teachers</a>
</nav>
</div>

{{ include('layout/footer.html.twig') }}